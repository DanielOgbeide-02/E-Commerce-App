import 'package:flutter/cupertino.dart';

class ClothesNewEntity{
  String ? label;
  String ? price;
  String ? name;
  String ? category;
  String ? clothItemPath;
  int ? quantity;


  ClothesNewEntity({
    this.label,
    this.price,
    this.name,
    this.category,
    this.clothItemPath,
    this.quantity
  });

}